<%= form_for :session, url: sessions_path do |f| %>
  <%= f.label :username, 'Enter a username' %><br/>
  your current ID : <%=current_user.email %>
  <%= f.hidden_field :username %><br/>
  <%= f.submit 'Start chatting' %>
<% end %>